<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <include
        android:id="@+id/rank_title"
        android:layout_alignParentTop="true"
        layout="@layout/title_no_logo" />

    <TabHost
        android:id="@+id/rank_tabhost"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_alignParentLeft="true"
        android:layout_below="@+id/rank_title" >

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical" >

            <TabWidget
                android:id="@android:id/tabs"
                android:layout_width="match_parent"
                android:layout_height="wrap_content" >
            </TabWidget>

            <FrameLayout
                android:id="@android:id/tabcontent"
                android:layout_width="match_parent"
                android:layout_height="match_parent" >

                <ListView
                    android:id="@+id/benchang_list"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:divider="@color/qc_room_background"
                    android:dividerHeight="1dp"
                    android:listSelector="@color/qc_room_trans" >
                </ListView>

                <ListView
                    android:id="@+id/zhoubang_list"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:divider="@color/qc_room_background"
                    android:dividerHeight="1dp"
                    android:listSelector="@color/qc_room_trans" >
                </ListView>

                <ListView
                    android:id="@+id/yuebang_list"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:divider="@color/qc_room_background"
                    android:dividerHeight="1dp"
                    android:listSelector="@color/qc_room_trans" >
                </ListView>

                <ListView
                    android:id="@+id/chaojibang_list"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:divider="@color/qc_room_background"
                    android:dividerHeight="1dp"
                    android:listSelector="@color/qc_room_trans" >
                </ListView>
            </FrameLayout>
        </LinearLayout>
    </TabHost>

</RelativeLayout>